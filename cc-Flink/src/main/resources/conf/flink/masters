
# 主节点机器（JobManager作业管理器）
bd91:8081


############### 任务执行Linux指令 ###############
# flink run
# -m master:8081
# -p 插槽数
# -c jar的Main-Class jar包
# --hostname 监听的主机ip --port 监听的主机端口号
###############################################